import { ArrowUpRight, Github, Linkedin, Mail } from 'lucide-react';
import { profile, githubUrl } from '../data/profile';
import { ExternalLink, Reveal } from './UI';
export default function Contact() { const contact = profile.email ? `mailto:${profile.email}` : profile.linkedinUrl; return <section id="contato" className="section container contact"><Reveal><span className="eyebrow">05 / A PRÓXIMA IDEIA PODE COMEÇAR AQUI</span><h2>Vamos construir<br /><span>alguma coisa?</span></h2><p>Estou sempre interessado em tecnologia, desenvolvimento backend, automação e projetos que resolvam problemas reais.</p><ExternalLink href={contact} className="button primary" label="Entrar em contato">Entrar em contato <ArrowUpRight size={19}/></ExternalLink>{!contact && <p className="contact-pending">Canais de contato em breve.</p>}<div className="contact-links"><ExternalLink href={githubUrl} label="GitHub"><Github size={18}/> GitHub</ExternalLink><ExternalLink href={profile.linkedinUrl} label="LinkedIn"><Linkedin size={18}/> LinkedIn</ExternalLink><ExternalLink href={profile.email ? `mailto:${profile.email}` : ''} label="Email"><Mail size={18}/> Email</ExternalLink></div></Reveal></section>; }
